import { Grid, Typography } from '@mui/material'

const AccountSettings = () => {
  return (
    <Grid container spacing={6}>
      <Grid item xs={12}>
        <Typography>This is account test page</Typography>
      </Grid>
    </Grid>
  )
}

export default AccountSettings
